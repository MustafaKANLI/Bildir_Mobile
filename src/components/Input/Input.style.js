import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        borderWidth: 1,
        padding: 5,
        margin: 10,
        borderRadius: 5,
        backgroundColor: 'white',
    }
})