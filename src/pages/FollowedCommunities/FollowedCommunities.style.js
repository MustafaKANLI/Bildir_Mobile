import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        borderColor: 'black',
        borderWidth: 1,
        borderRadius: 5,
        padding: 10,
        margin: 10
    },
    title: {
        fontSize: 18,
        color: 'black'
    }
})